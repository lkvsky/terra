includes:
  - secrets.yaml

runtime: nodejs22
env: standard
instance_class: F2

automatic_scaling:
  max_instances: 5
  min_instances: 0
  target_cpu_utilization: 0.65

env_variables:
  NODE_ENV: production
  
handlers:
  - url: /.*
    script: auto
    secure: always
